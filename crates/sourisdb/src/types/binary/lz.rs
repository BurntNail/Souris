use crate::types::binary::BinarySerError;
use crate::utilities::cursor::Cursor;

pub fn lz (bytes: Vec<u8>) -> Vec<u8> {
    todo!()
}

pub fn un_lz (len: usize, cursor: &mut Cursor<u8>) -> Result<Vec<u8>, BinarySerError> {
    todo!()
}